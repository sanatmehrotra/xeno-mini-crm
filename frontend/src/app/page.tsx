/**
 * src/app/page.tsx
 *
 * The "/" route is handled by src/app/(dashboard)/page.tsx via the route group.
 * This file only exists to satisfy Next.js type generation.
 * It should never actually render — the (dashboard) layout's auth guard runs first.
 */
export { default } from "./(dashboard)/page";
